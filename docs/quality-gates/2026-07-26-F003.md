---
feature_ids: [F003]
related_features: [F001, F002]
topics: [quality-gate, cache, indexeddb, concurrency, progress, browser-acceptance]
doc_kind: verification
created: 2026-07-26
---

# F003 Quality Gate Report

Spec: `docs/features/F003-merged-pr-cache-performance.md`

原始需求: `docs/discussions/2026-07-26-F003-cache-performance-design/README.md`

检查时间: 2026-07-26 19:45 Asia/Shanghai

实现证据 HEAD: `8766a99`

## 愿景覆盖

| operator 原始需求 | AC | 实现与证据 |
|---|---|---|
| 合入 PR 无需为极少见的合入后评论变化浪费兼容复杂度 | AC-A1, AC-A2 | 整颗 PR 永久缓存；仅 full + 当前 schema 命中 |
| 按 PR 持久缓存，重复查询少拉远端 | AC-A1, AC-A3 | IndexedDB close/reopen 测试；dogfood 复查为缓存 1、网络 0 |
| 使用 8/16 并行，不能放大为 24/48 请求 | AC-B1 | `_fetch` 共享平台级 `ProviderRequestBudget`；并发仪表测试覆盖 |
| 结果出现后进度必须进入可信终态 | AC-B2, AC-B3 | settle 状态机与 full/partial/failed/cancelled DOM 断言 |
| 用户资料变化不应重新查询远端 | AC-C1 | 单一 remap 路径；CRUD/toggle/import wiring 与 fetch=0 测试 |

## CloseGateReport

```yaml
close_gate_report:
  feature_id: F003
  spec_path: docs/features/F003-merged-pr-cache-performance.md
  head_sha: 8766a99
  report_date: 2026-07-26
  ac_matrix:
    - ac_id: AC-A1
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 GitHub/GitCode PR core cache integration" }
        - { kind: screenshot, ref: "/tmp/cat-cafe-evidence/code-statistics-f003/02-cache-hit.png" }
      resolution: null
    - ac_id: AC-A2
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 PR cache key and lifecycle" }
        - { kind: commit, ref: "d000771" }
      resolution: null
    - ac_id: AC-A3
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 IndexedDB persistence" }
        - { kind: doc, ref: "README.md:34" }
      resolution: null
    - ac_id: AC-B1
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 provider request budgets" }
        - { kind: commit, ref: "b585358" }
      resolution: null
    - ac_id: AC-B2
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 query progress state machine" }
        - { kind: screenshot, ref: "/tmp/cat-cafe-evidence/code-statistics-f003/02-cache-hit.png" }
      resolution: null
    - ac_id: AC-B3
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 query progress state machine" }
        - { kind: commit, ref: "f3a1299" }
      resolution: null
    - ac_id: AC-C1
      status: met
      evidence:
        - { kind: test, ref: "test.html:F003 local user remapping" }
        - { kind: screenshot, ref: "/tmp/cat-cafe-evidence/code-statistics-f003/03-local-remap.png" }
      resolution: null
    - ac_id: AC-C2
      status: met
      evidence:
        - { kind: test, ref: "test.html — 222 passed, 0 failed" }
      resolution: null
    - ac_id: AC-C3
      status: met
      evidence:
        - { kind: screenshot, ref: "/tmp/cat-cafe-evidence/code-statistics-f003/01-first-query.png" }
        - { kind: screenshot, ref: "/tmp/cat-cafe-evidence/code-statistics-f003/02-cache-hit.png" }
        - { kind: screenshot, ref: "/tmp/cat-cafe-evidence/code-statistics-f003/03-local-remap.png" }
      resolution: null
```

Release acceptance remains a separate hard gate: F003 status cannot become `done` until the final `main` SHA is live on GitHub Pages and the production journey passes.

## Functional verification

| Requirement | Code | Test evidence | Status |
|---|---|---|---|
| Whole merged PR cache; full/schema/identity gates | `index.html:792-1010` | cache lifecycle, IndexedDB reopen, provider cache integration | ✅ |
| GitHub 8 / GitCode 16 request-level budget | `index.html:728-790` | instrumented peaks respect both limits and exceed 3 | ✅ |
| Settle-based terminal progress and partial reasons | `index.html:1921-1973`, `1975-2180` | progress state machine + DOM terminal assertions | ✅ |
| Local user remapping without network | `index.html:2568-2575`, mutation call sites | remap + mutation wiring + fetch=0 | ✅ |
| User-facing storage boundary | `index.html:392-393`, `README.md:34-35` | disclosure assertions | ✅ |

## Patch counter

Eight candidate commits were inspected. Cache storage, provider budgets, query integration/progress, user remap, partial diagnostics, disclosure, fallback-boundary refactor, and fresh-context finding resolution are distinct AC increments or review-driven corrections. Same-bug rework count is 0; the hard threshold is not reached.

## Fallback layer check

Project-specific manual diff audit was used because this standalone repository has no fallback scanner script.

- IndexedDB initialization failure → one Memory backend, preserving query availability.
- Cache read failure → one miss, preserving correctness by using the provider.
- Cache write failure → persistence is skipped; the current query result remains valid.
- Provider-level duplicate cache-read catches were removed in `e03843e`.

This is one persistent coordinate plus one memory fallback; there is no localStorage cache, TTL hierarchy, compression layer, or provider-level duplicate fallback.

## Fresh-Context Findings

Agent: `[宪宪/claude-opus-4-6🐾]`

Scanned SHA: `2923d67`

| Finding | Author disposition | Evidence |
|---|---|---|
| FC-1 GitHub `networkPRs` counted filtered PRs | fixed | `8766a99`; cache + network now equals selected PR total |
| FC-2 empty cached base branch could be treated as structurally valid | fixed | CacheStore requires non-empty base branch and valid merged time |
| FC-3 abort/cache write TOCTOU | contract clarified | UI cancellation never writes success; already completed full stable PR data remains safe to cache |
| FC-4 user mutation rebuilt filter dropdowns twice | fixed | remap reuses the refresh already performed by `renderUsers`; one-refresh integration test |
| FC-5 `Map delta: none` with new CacheStore | dismissed | CacheStore is an internal component of the existing standalone single-file cell; no owner, boundary, extension point, or canonical anchor changes |
| FC-6 GitHub filtered network count lacked test | fixed | `F003 GitHub network PR count precision` |
| FC-7 parallel repository discovery lacked test | fixed | `F003 repository discovery runs in parallel` |
| FC-8 nested collaboration fan-out lacked budget test | fixed | 12 concurrent GitHub PR collaboration calls stay within shared limit 8 |
| FC-9 `_cacheHit` path lacks a second activity validation | dismissed | production `_cacheHit` is private provenance set only from validated, cloned `CacheStore.get` output; duplicating the same gate would create a second contract owner |
| FC-10 cache entry factory hard-coded full | fixed | factory now preserves PR completeness; CacheStore remains the final full-only gate |

Red/green evidence: `216 passed, 6 failed` → `222 passed, 0 failed`.

## Architecture ownership

- Architecture cell: `standalone-single-file-app` (`MAINTAINING.md` §2)
- Map delta: `none`
- Why: CacheStore and request budgets stay inside the existing Storage / Provider / StatsEngine / Query Controller boundary.
- Mechanical warning for reviewer: the diff introduces the name `CacheStore` while Map delta is `none`; confirm that keeping it inside the single-file application cell is semantically correct.

## Dogfood-Your-Slice

Scope verdict: ✅ 必做

- Worktree/CWD: `/Users/lang/workspace/github/code-statistics-f003-merged-pr-cache`
- URL: `http://127.0.0.1:8901/index.html?dogfood=f003`
- Fixture path: production `runQuery()` with deterministic GitHub API responses; browser IndexedDB was cleared before first run.
- First query: 1 PR（缓存 0 · 网络 1）；PR detail 与三类协作来源各请求 1 次。
- Second query: 1 PR（缓存 1 · 网络 0）；仓库发现继续请求，PR detail 与三类协作来源计数均未增加。
- User edit: Alice → Alice Renamed；当前汇总立即更新，全部网络计数不变。
- IndexedDB mode: `indexeddb`.
- Bugs found: none.

## Visual evidence

| Requirement | Evidence |
|---|---|
| First network query reaches terminal state | `/tmp/cat-cafe-evidence/code-statistics-f003/01-first-query.png` |
| Repeated query reports cache 1 / network 0 | `/tmp/cat-cafe-evidence/code-statistics-f003/02-cache-hit.png` |
| User edit remaps the current table locally | `/tmp/cat-cafe-evidence/code-statistics-f003/03-local-remap.png` |
| 15-second interaction record | `/tmp/cat-cafe-evidence/code-statistics-f003/04-dogfood-15s.mp4` (`ffprobe`: 15.000000 seconds) |

## Design and artifact hygiene

- `.pen` glob result: no files.
- Design comparison: no dedicated design artifact; the change preserves the existing three-tab layout and only changes progress text and storage disclosure.
- Root media/design artifacts in worktree status: none.
- Root media/design artifacts in `origin/main...HEAD`: none.

## Fresh verification

| Check | Result |
|---|---|
| `node --check` extracted production script | exit 0 |
| `node --check` extracted test script | exit 0 |
| `git diff --check` | exit 0 |
| Chrome `test.html?green=fresh-context-3` | 222 passed, 0 failed |
| Fresh Chrome application load | HTTP 200; 0 console/page errors |
| Root artifact hygiene, worktree + committed diff | no matches |
| Local `.pen` glob | no matches |

This repository has no `package.json`, build step, lint command, architecture checker, capability-tip checker, hotfix scanner, or fallback scanner. The project-specific checks defined by `docs/SOP.md` were run instead. F003 declares `tips_exempt` because it is a standalone local tool and does not use the Clowder AI tips system.
